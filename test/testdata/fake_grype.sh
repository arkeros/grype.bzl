#!/usr/bin/env bash
# Stands in for the grype binary so grype_scan can be analysed without a
# toolchain or a vulnerability database. Never executed: these are analysis
# tests, which stop before any action runs.
exit 0
